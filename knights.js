function knight(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function superKnight(square, coords, dotThis, piece) {
  let possibleMoves = moves.superKnight;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function longKnight(square, coords, dotThis, piece) {
  let possibleMoves = moves.longKnight;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function elephant(square, coords, dotThis, piece) {
  let possibleMoves = moves.elephant;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_atomic(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_atomic;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_ghoul(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_ghoul;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_ghost(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_ghost;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_murderous(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_murderous;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function royalKnight(square, coords, dotThis, piece) {
  let possibleMoves = moves.royalKnight;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function camel_knight(square, coords, dotThis, piece) {
  let possibleMoves = moves.camel_knight;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
/* 10 */
function disabledKnight(square, coords, dotThis, piece) {
  let possibleMoves = moves.disabledKnight;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function spiderKnight(square, coords, dotThis, piece) {
  let possibleMoves = moves.spiderKnight;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function threeElephant(square, coords, dotThis, piece) {
  let possibleMoves = moves.threeElephant;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_defuser(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_defuser;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_reverseAtomic(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_reverseAtomic;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_phantom(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_phantom;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_crowding(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_crowding;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_stacking(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_stacking;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_promotion(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_promotion;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_infinite(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_infinite;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
/* 20 */
function knight_capturetheflag(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_capturetheflag;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_cloning(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_cloning;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_onemove(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_onemove;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_shared(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_shared;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}
function knight_fragile(square, coords, dotThis, piece) {
  let possibleMoves = moves.knight_fragile;
  dotAllMovesNoJump(piece, possibleMoves, coords, dotThis, square);
}